__pycache__
.mypy_cache
.DS_Store
anki.prof
target
/user.bazelrc
.dmypy.json
/.idea/
/.vscode
/.bazel
/windows.bazelrc
/out
node_modules
.n2_db
.ninja_log
.ninja_deps
/extra
yarn-error.log
ts/.svelte-kit
.yarn
.claude/settings.local.json
.claude/user.md
.venv
docs/_build/
docs/autoapi/
.complexipy_cache
# Personal, machine-local Cursor rules (keep out of version control)
.cursor/rules/personal.mdc
# Local secrets (e.g. OPENAI_API_KEY for the AI evals). NEVER commit.
.env
# Held-out real SOA sample questions are copyrighted: keep them local, never
# commit them. The committed original fallback (pylib/anki/speedrun/sample_items.json)
# is what ships. See data/soa_sample_p/README.md.
/data/soa_sample_p/*.json
# Copyrighted study material (e.g. ACTEX / textbook PDFs) is reference-only and
# must NEVER be committed to this AGPL repo. Keep such files local.
*.pdf
# Local design-tool (impeccable) session recordings — not project source.
.impeccable/
