# Optional dependency for the Friday AI layer (Feature 1 classifier + Feature 2
# generation). AI is OFF by default and the app scores without it, so this is not
# required to build or run the app or the non-AI evals.
#
#   out/pyenv/bin/pip install -r tools/speedrun/requirements-ai.txt
#   export OPENAI_API_KEY=...      # then: make ai-eval
#
# Pinned to the 1.x line: the client uses the 1.x OpenAI() + chat.completions API
# (see anki/speedrun/ai.py); 2.x is untested here. Tested with 1.109.1.
openai>=1.40,<2
